import { TsShopFilterPipe } from './ts-shop-filter.pipe';

describe('TsShopFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TsShopFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
